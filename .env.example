# ─────────────────────────────────────────────
#  Lyric Video Studio — Production .env
# ─────────────────────────────────────────────
#  Copy to the repo root: cp .env.example .env
#  Used by docker-compose.yml

# Cloudflare Tunnel token (from Zero Trust dashboard)
TUNNEL_TOKEN=your_cloudflare_tunnel_token_here
